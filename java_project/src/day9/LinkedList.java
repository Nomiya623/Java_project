package day9;

import java.awt.Shape;

public class LinkedList {
//리스트, 링크드 리스트
	//start 첫 시작 변수 
	//last 다음 임력 변수 주소
	//obj 다음 입력 변수 임시로 저장하는 장소
	//start, obj, last
	//start
	 public static void main(String[] args) {
	        Shape start, last, obj;

	        // Create a linked list of shapes
//	        start = new Line();  // Line object connected
//	        last = start;
//	        obj = new Rect();
//	        last.next = obj;     // Rect object connected
//	        last = obj;
//	        obj = new Line();     // Another Line object connected
//	        last.next = obj;
//	        last = obj;
//	        obj = new Circle();   // Circle object connected
//	        last.next = obj;
//
//	        // Draw all shapes in the linked list
//	        Shape p = start;
//	        while (p != null) {
//	            p.draw();
//	            p = p.next;
//	        }
	    }
	}
